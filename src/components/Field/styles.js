import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    view: {
        marginTop: 20,
    },
    label: {

    },
    input: {
        borderColor: '#000',
        borderWidth: 1,
        borderRadius: 8,
        width: 300,
        height: 40,
        padding: 10,
        marginTop: 5,
    }
});